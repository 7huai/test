first line to test
The second line to test

很明显，退回到上一版本后，本地工作库是不会改变的，以前一直以为是会变的。所以正常的工作流是退回上一版本后，修改现有的本地工作库，再次上传。
使用git diff filename可以查看当前文件与库中的比较，看修改了哪些内容。


版本2.1
版本2.2
版本2.3

使用git reset --hard 版本号可以更新想要的版本

使用git checkout filename 可以将工作区的文件（使用git add）在工作区未提交的更改删除。

// ------------
在分支master中添加内容
// ----------
在分支dev中添加内容

在分支bug中添加内容
conflict line
